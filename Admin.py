class Admin
   def_init_(self,name,accountid)->None:
slef.name
self.manager=None

    def total_balance(self,name,amount)
      self.balance=amount.money
      return balance_amount.money

def loan(self,amount,money)
   if amount<self.balance:
      self.loan+=amount
      self.balance-=amount

      print(+'loan{amount}for(money)')
      
   else:
      print(f'not enough money to pay{amount})
            
 def loan_feature(self,balance):
    if account.balance<self.amount:
     

